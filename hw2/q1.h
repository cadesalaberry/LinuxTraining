#ifndef Q1_H
#define Q1_H

int pascalTriangle[100][100];


/**
 * in this function your supposed to build the pascal triangle, (filling element values to the global variable pascalTriangle
 * notice that you have to fill all 100 rows
 */
void buildPascalTriangle()
{
	//TODO: fill your code here	
}

/**
 * in this function, you are supposed to return the element specified by level, and eleidx, 
 *
 * e.g. GetPTElement(2, 1) will return 2
 * if (level, eleIdx) is referring to a 'non-exist' element, return -1;
 */
int GetPTElement(int level, int eleIdx)
{
	int ret = 0;
	//TODO: fill your code here
	return ret;
}

#endif
